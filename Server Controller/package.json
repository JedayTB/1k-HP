{
  "name": "server-controller",
  "packageManager": "yarn@4.5.2",
  "main": "dist/main.js",
  "scripts": {
    "start": "tsc && electron .",
    "build": "electron-packager . ServerControlPanel --platform=win32 --arch=x64 --out=dist --overwrite"
  },
  "dependencies": {
    "electron": "^34.0.0"
  },
  "devDependencies": {
    "electron-packager": "^17.1.2"
  },
  "workspaces": [
    "packages/*"
  ]
}
